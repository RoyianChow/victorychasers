<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Join Tournament</title>
</head>
<body>
  <h1>Join Tournament: <%= tournament.name %></h1>

  <form action="/tournaments/join/<%= tournament._id %>" method="POST">
    <div class="form-group">
      <label for="playerName">Player Name</label>
      <input type="text" class="form-control" id="playerName" name="playerName" placeholder="Enter your name" required>
    </div>

    <button type="submit" class="btn btn-primary">Submit</button>
    <a href="/tournaments/view/<%= tournament._id %>" class="btn btn-warning">Cancel</a>
  </form>

</body>
</html>
